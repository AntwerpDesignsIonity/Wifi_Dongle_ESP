# Generated by firmware/certs/gen_certs.py — do NOT commit the private key
server_key.pem
# The cert CAN be committed (it's public), but is excluded here
# so gen_certs.py is always run fresh per build environment.
# Remove the next line if you want to track the cert in git.
server_cert.pem
